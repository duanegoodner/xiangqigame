from game_rules import opponent_of
from board_utilities import in_same_rank, in_same_file
from game_piece import GamePiece
from game_board import GameBoard


def chariot(board: GameBoard, curr_position: tuple):
    return set()


def horse(board: GameBoard, curr_position: tuple):
    return set()


def elephant(board: GameBoard, curr_position: tuple):
    return set()


def advisor(board:)